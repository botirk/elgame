{
  "name": "elgame",
  "version": "1.0.0",
  "description": "English Learning Game",
  "scripts": {
    "generate": "cd ./src/compileTime && npx ts-node -e \"require('./generateWords').default();\"",
    "develop": "npm run generate && npx parcel src/examples/onePageDev.html --host 0.0.0.0 --log-level verbose",
    "build": "npm run generate && npx parcel build src/examples/onePage.html --dist-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.7.0",
    "ts-node": "^10.9.1"
  }
}
